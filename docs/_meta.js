// Section navigation for the RAMMP docs site.
//
// The hub copies this file in alongside the pages; its own copy is generated and
// gitignored, so THIS is the source of truth for ordering and titles.
//
// Ordered for a newcomer: start, then the headset setup you do once, then the
// reference, with troubleshooting last so it is findable when something breaks.
// docs/superpowers/ (design specs and plans) is excluded in the hub's sources.yml.
export default {
  index: 'Introduction',
  'getting-started': 'Getting started',
  'quest-setup': 'Setting up the Quest',
  interface: 'Interface reference',
  troubleshooting: 'Troubleshooting'
}
